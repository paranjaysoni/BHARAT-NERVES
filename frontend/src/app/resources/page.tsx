import { PageHeader } from "@/components/shared/PageHeader";
import { SectionCard } from "@/components/shared/SectionCard";

export default function ResourcesPage() {
  return (
    <div className="space-y-6">
      <PageHeader
        title="Resources"
        description="Future repository for corridor assets, facilities, response capacity, and reference material."
      />
      <SectionCard title="Resources Skeleton">Page skeleton ready.</SectionCard>
    </div>
  );
}
